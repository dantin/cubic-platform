language: java

services:
  - docker

jdk:
  - openjdk8

env:
  - DOCKER_COMPOSE_VERSION=1.21.0

before_script:
# - sudo redis-server /etc/redis/redis.conf --requirepass 'password'
#  - sudo rm /usr/local/bin/docker-compose
#  - curl -L https://github.com/docker/compose/release/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
#  - chmod +x docker-compose
#  - sudo mv docker-compose /usr/local/bin
  - docker-compose up -d
  - ./gradlew bootJar
