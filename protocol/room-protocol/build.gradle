
dependencies {
    compile project(":protocol:common-protocol")
    compile("com.fasterxml.jackson.core:jackson-annotations:${jacksonAnnotationsVersion}")
    compile("com.fasterxml.jackson.core:jackson-databind:${jacksonAnnotationsVersion}")

    testCompile("junit:junit:${junitVersion}")
}
